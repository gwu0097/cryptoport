NEXT_PUBLIC_SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# HTTP Basic Auth gate (src/proxy.ts) — required in every environment,
# including local dev. Missing/empty means every request is denied.
BASIC_AUTH_USER=
BASIC_AUTH_PASSWORD=

NEXT_PUBLIC_SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# The HTTP Basic Auth gate (src/proxy.ts) checks credentials stored in
# cryptoport.app_credentials, not env vars — see db/schema.sql for the seed
# row and /settings to change them.
